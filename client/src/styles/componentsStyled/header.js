import styled from "styled-components";

//componente generales
import {} from "../generalComponents.js";

export const ContainerHeader = styled.div`
  position: relative;
  padding: 5rem 0;
  background: ${({ theme }) => theme.colors.first};
`;